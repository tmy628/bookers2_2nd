<%= render '/users/list', user: @user, book: @book %>

<h2>Users</h2>

<% @users.each do |f| %>
  <%= attachment_image_tag f, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
  <%= f.name %>
  <%=link_to "Show", user_path(f.id) %>
<% end %>